[project]
name = "bumble"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "build123d",  # parametric CAD
    "trimesh",    # mesh interference checks
    "pyvista",    # render generation
    "matplotlib>=3.11.0",
]

[tool.uv]
package = false
